import { createSlice } from "@reduxjs/toolkit";

const suggestions = createSlice({
    name : 'suggestions',
    initialState:{
        results:[]
    },
    reducers:{
        queries:() => {
         state.results.add
        }
    }
})